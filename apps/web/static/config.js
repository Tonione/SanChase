window.__SANCHASE_CONFIG__ = {
  // Overridden at runtime by the web/gateway server from SANCHASE_DEV.
  // Set wsUrl in production if backend lives on another domain.
  wsUrl: "",
  devMode: false
};
